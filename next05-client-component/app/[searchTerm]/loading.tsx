// note(roy): applies to entire route now, not part of the page
// note(roy): show loading due to react suspense.
import React from "react";

export default function loading() {
  return (
    <main>
      <h2>Loading...</h2>
    </main>
  );
}
